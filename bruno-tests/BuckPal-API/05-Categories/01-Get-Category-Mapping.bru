meta {
  name: Get Category Mapping
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}{{apiPath}}/categories/mapping
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain category mappings", function() {
    const data = res.getBody();
    expect(data).to.be.an('object');
    // Should contain budget category mappings
    expect(Object.keys(data).length).to.be.greaterThan(0);
  });
}